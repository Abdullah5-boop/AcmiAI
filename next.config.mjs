/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */

  images: {
    domains: ['picsum.photos', 'fastly.picsum.photos'], // add all external hostnames here
  },
};

export default nextConfig;
